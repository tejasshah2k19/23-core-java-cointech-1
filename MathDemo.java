import java.lang.*; //default statement in all .class file 
import java.util.Scanner;

public class MathDemo {

    public static void main(String[] args) {

        // Math
        // java.lang
        // Scanner scr = null;
        // scr.nextInt();
        System.out.println(Math.sqrt(9));
        System.out.println(Math.sqrt(8));

        System.out.println(Math.round(2.8288888833232));
        System.out.println(Math.round(2.4288888833232));

        // how ? why ?

        // all the methods are static in Math class
        // so we never create object of Math class

        // what if i create object of Math class?

        // Math obj = new Math();
        // how to prevent object creation of a class?

    }
}
